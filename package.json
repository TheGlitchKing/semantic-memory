{
  "name": "@theglitchking/semantic-memory",
  "version": "1.5.1",
  "description": "Unified memory-layer plugin for Claude Code. One MCP server, multiple typed corpora (vault, code, plans, docs, research, project-map), unified cross-corpus knowledge graph, conditional tool registration, in-process drift detection, and human↔code translation. Rebrand of @theglitchking/semantic-sidekick at v1.0 — see CHANGELOG for migration.",
  "type": "module",
  "main": "./dist/core/index.js",
  "types": "./dist/core/index.d.ts",
  "exports": {
    ".": "./dist/core/index.js",
    "./mcp": "./dist/mcp/server.js"
  },
  "bin": {
    "semantic-memory": "bin/semantic-memory",
    "semantic-sidekick": "bin/semantic-sidekick"
  },
  "files": [
    "bin",
    "dist",
    ".claude-plugin",
    "hooks",
    "commands",
    "docs",
    "scripts/link-skills.js",
    "scripts/pre-commit-lint.sh",
    "skills/semantic-first",
    "skills/vault-first",
    "skills/research-mode",
    "skills/outage-silence",
    "README.md",
    "CHANGELOG.md",
    "ROADMAP.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "eval": "vitest run test/retrieval-eval",
    "lint": "tsc --noEmit",
    "prepublishOnly": "npm run build",
    "postinstall": "node scripts/link-skills.js"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "memory-layer",
    "semantic-search",
    "knowledge-graph",
    "multi-corpus",
    "markdown",
    "obsidian",
    "vector-search",
    "embeddings",
    "wikilinks",
    "claude",
    "llm",
    "ai",
    "notes",
    "vault",
    "code-search",
    "drift-detection",
    "translation"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "optionalDependencies": {
    "onnxruntime-node": "^1.24.3"
  },
  "dependencies": {
    "@huggingface/transformers": "^4.0.1",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "@theglitchking/claude-plugin-runtime": "^0.1.0",
    "chokidar": "^5.0.0",
    "commander": "^14.0.3",
    "glob": "^13.0.6",
    "graphology": "^0.26.0",
    "graphology-library": "^0.8.0",
    "graphology-shortest-path": "^2.1.0",
    "graphology-traversal": "^0.3.1",
    "gray-matter": "^4.0.3",
    "hnswlib-node": "^3.0.0",
    "minimatch": "^10.2.5",
    "onnxruntime-web": "^1.24.3",
    "remark-parse": "^11.0.0",
    "remark-wiki-link": "^2.0.1",
    "unified": "^11.0.5",
    "yaml": "^2.4.0"
  },
  "devDependencies": {
    "@theglitchking/hit-em-with-the-docs": "latest",
    "@types/node": "^25.5.2",
    "tsup": "^8.5.1",
    "typescript": "^6.0.2",
    "vitest": "^4.1.2"
  },
  "comments": {
    "overrides": "Pin patched versions of transitive deps to keep this package's supply chain clean (v1.5.1). All are same-MAJOR bumps — no breaking changes. NOTE: npm ignores a dependency's overrides in downstream installs, so consuming projects that gate CI on `npm audit` should mirror this block or run `npm audit fix`. Every advisory here is unreachable in practice: this is a local stdio MCP server + hook scripts processing the user's own files — the hono/express-rate-limit/ip-address/qs cluster is the MCP SDK's HTTP transport, which this plugin never instantiates (stdio only). js-yaml (gray-matter) and fast-uri (ajv) are deliberately NOT overridden: their only fixes are MAJOR bumps that would break frontmatter parsing / MCP tool-input validation, and the DoS vectors require attacker-controlled input this plugin never accepts."
  },
  "overrides": {
    "protobufjs": "7.6.5",
    "@protobufjs/utf8": "1.1.2",
    "@xmldom/xmldom": "0.9.10",
    "brace-expansion": "5.0.7",
    "qs": "6.15.3",
    "ip-address": "10.2.0",
    "express-rate-limit": "8.5.2",
    "hono": "4.12.29",
    "@hono/node-server": "1.19.14"
  }
}
